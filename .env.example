# Azure OpenAI Configuration for Resume Screening System
# =====================================================
# Copy this content to a file named '.env' in the same directory

# Required Environment Variables
AZURE_OPENAI_API_KEY=your_azure_openai_api_key_here
AZURE_OPENAI_ENDPOINT=https://your-resource-name.openai.azure.com/

# Optional Environment Variables (with defaults)
AZURE_OPENAI_DEPLOYMENT=gpt-4
AZURE_OPENAI_API_VERSION=2024-02-01
LOG_LEVEL=INFO

# Instructions:
# 1. Create a file named '.env' in the project root directory
# 2. Copy the content above into the .env file
# 3. Replace 'your_azure_openai_api_key_here' with your actual API key
# 4. Replace 'your-resource-name' with your Azure OpenAI resource name
# 5. Install python-dotenv: pip install python-dotenv
# 6. Run the backend: python resumematching.py
# 7. Run the frontend: npm run dev
